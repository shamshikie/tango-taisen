@page
@model EnableAuthenticatorModel
@{
    ViewData["Title"] = "認証アプリの構築";
    ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}
<style>
    .authenticator-list {
        list-style: none;
        padding: 0;
    }

    .authenticator-box {
        display: flex;
        flex-direction: column;
        gap: 10px;
        padding: 10px;
        background: #f8f9fa;
        border-radius: 8px;
    }

    .authenticator-item {
        padding: 10px;
        border: 1px solid #ddd;
        border-radius: 6px;
        background: #fff;
    }
</style>
<partial name="_StatusMessage" for="StatusMessage" />
<h3 class="mb-4">@ViewData["Title"]</h3>
<div>
    <p>認証アプリを有効にする方法:</p>
    <ol class="list">
        <li class="authenticator-list">
            <p class="mb-2">以下の2つの認証アプリのうち、どちらかをダウンロードしてください。</p>

            <div class="authenticator-box">
                <div class="authenticator-item">
                    <h6 class="fw-bold text-primary">📱 Microsoft Authenticator</h6>
                    <a href="https://go.microsoft.com/fwlink/?Linkid=825072" class="btn btn-outline-primary btn-sm">Android版</a>
                    <a href="https://go.microsoft.com/fwlink/?Linkid=825073" class="btn btn-outline-primary btn-sm">iOS版</a>
                </div>

                <div class="authenticator-item">
                    <h6 class="fw-bold text-danger">📱 Google Authenticator</h6>
                    <a href="https://play.google.com/store/apps/details?id=com.google.android.apps.authenticator2&amp;hl=en"
                       class="btn btn-outline-danger btn-sm">Android版</a>
                    <a href="https://itunes.apple.com/us/app/google-authenticator/id388497605?mt=8"
                       class="btn btn-outline-danger btn-sm">iOS版</a>
                </div>
            </div>
        </li>
        <li>
            <p>QR コードをスキャンするか、このキーを  <kbd>@Model.SharedKey</kbd> 二要素認証アプリに入力してください。</p>
            <div class="alert alert-info">
                QRコード生成を有効にする方法は<a href="https://go.microsoft.com/fwlink/?Linkid=852423">こちら</a>
            </div>
            <div id="qrCode"></div>
            <div id="qrCodeData" data-url="@Model.AuthenticatorUri"></div>
        </li>
        <li>
            <p>
                QRコードをスキャンするか、上記のキーを入力すると、二要素認証アプリが固有のコードを提供します。 下の確認ボックスにコードを入力してください。
            </p>
            <div class="row">
                <div class="col-md-12">
                    <form id="send-code" method="post">
                        <div class="form-floating mb-3">
                            <input asp-for="Input.Code" class="form-control" autocomplete="off" placeholder="Please enter the code."/>
                            <label asp-for="Input.Code" class="control-label form-label">認証コード</label>
                            <span asp-validation-for="Input.Code" class="text-danger"></span>
                        </div>
                        <button type="submit" class="w-100 btn btn-lg btn-brand">認証</button>
                        <div asp-validation-summary="ModelOnly" class="text-danger" role="alert"></div>
                    </form>
                </div>
            </div>
        </li>
    </ol>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
